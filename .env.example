# Docker Compose Environment Variables
# Copy this file to .env and update with your local values
# NEVER commit .env to git!

# PostgreSQL Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_DB=menago_task_dev
POSTGRES_PORT=5433

# Instructions:
# 1. Copy this file: cp .env.example .env
# 2. Update POSTGRES_PASSWORD with a secure password (min 8 characters)
# 3. Make sure .env is in .gitignore (it should be already)
